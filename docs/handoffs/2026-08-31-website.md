# 2026-08-31 — WEBSITE (architect/Cowork session, edited directly per Koren)

## What changed
Replaced the static tabbed demo section (`#demo`, "One lead, four minutes") on both
`website/index.html` and `website/he/index.html` with a scroll-driven lead-journey
section: 6 steps + sticky phone mockup whose screen swaps per step, followed by a
recap grid and a dark outro CTA band (`.jn-outro`, CTA -> #pricing).

- `website/assets/styles.css` — appended `jn-`prefixed block at end of file
  (prefixed to avoid collisions with existing .card/.pane/.cta/.wave/.field/.meter).
  Old demo CSS (.tabs/.stage/.tline/.pane/.out/.field/.meter usage in demo) is now
  partly dead code — left in place deliberately, cleanup is a separate pass.
- `website/assets/site.js` — appended journey scroll engine (IIFE, no-ops if the
  section is absent). Old tab JS left in place, now inert.
- Anchor `#demo` and nav links preserved. Reduced-motion respected (jn override
  block at end of styles.css). Phone column is aria-hidden (duplicate of step text).

## Copy rules applied (Koren)
- No em-dashes anywhere in site copy (AI tell). New section is clean; REST OF THE
  SITE still has them (hero, capabilities, meta tags) — pending cleanup task.
- Agent is generic ("הסוכן הקולי", not KEREN) — each tenant names their own agent.

## Resolved
Step 6 (no-answer -> WhatsApp + scheduled redial) CONFIRMED by Koren 2026-08-31.
Added step 7 (same date, per Koren): in-call callback scheduling ("call me in an
hour" -> callback at the requested time) + adaptive follow-up pacing (cooperative
lead = fast track, hesitant lead = softer, spaced-out follow-ups). Journey is now
7 steps / 7 panes; times array in site.js updated accordingly.

## Not done
- git commit/push + Netlify deploy — Koren does this himself.
- Em-dash cleanup across the rest of both pages.
- Removing dead demo CSS/JS.
