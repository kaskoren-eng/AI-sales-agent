# 2026-08-27 — voice: number/time speech, anti-repetition ledger, spoken register

Koren's three live-call complaints, shipped as three independent commits on
`feature/crm-automation`, each behind its own env kill-switch, **all default ON**:

| commit | what | kill-switch |
|---|---|---|
| 642a684 | Hebrew number/time speech normalizer — "16:30" spoken as "ארבע וחצי" | `VOICE_SPEECH_NUMBERS_ENABLED` |
| 1fd1880 | Anti-repetition: phrase ledger + variation banks + `repeatedPhraseCount` | `VOICE_PHRASE_LEDGER_ENABLED` |
| 9540308 | SPOKEN_REGISTER prompt section + light-slang bank (round-5 verified) | `VOICE_SPOKEN_REGISTER_ENABLED` |

## What shipped

1. **`speech-numbers.he.ts`** — deterministic digits→words in the speech path only
   (guardSpeech, BEFORE stripNiqqud + the gender tables; position pinned by test; transcripts
   keep the digits, same contract as the gender fix). Clock times on the colloquial feminine 12h
   clock incl. ranges + a conservative בערב heuristic (lone 18:00–21:59 only), phone digit
   read-out, round prices, bare 1–19 with a curated masculine-noun list. Prompt step-3 + the
   calendar tool guidance now model colloquial hours themselves — the normalizer is the net.
   49 unit tests + chunk-boundary + ordering pins.
2. **`phrase-ledger.ts`** — humanization §4's missing half. Per-call 4-gram ledger over committed
   assistant items (draft-echo deduped); a "do not reuse these phrasings" system note appended at
   the trimHistory-safe turn boundary with a stable item id (tail-append → prompt-cache prefix
   intact, no preemptive-draft invalidation — measured, below). Name question + all six
   discovery questions became INTENT + 5-phrasing banks. `summary.repeatedPhraseCount` added to
   CallReport; `scripts/repeated-phrases-baseline.mjs` recomputes it over old reports.
3. **SPOKEN_REGISTER** — structure-first simple-Hebrew rules + light-slang bank
   (סבבה, אחלה, מעולה, בקטנה, על הדרך), one touch per reply max, heavy slang banned by name
   (אין מצב/וואי/פצצה/מהמם/אש). Every bank word passed round-5 screening BEFORE entering.
   Slang words feed the ledger as tracked unigrams. Prompt tests in the same commit.

## Verification

- **Round 5** (`tests/hebrew-tts-niqqud-ab/round5.py` + `roundtrip5.ts` + `index-round5.html`,
  sonic-3.5, clips committed): **17/17 PASS** on the 8 kHz → Soniox round-trip — all eight time
  forms (ארבע וחצי, רבע לחמש, עשר וחמישה, ארבע ורבע, שמונה בערב, עשרה לחמש, ארבע ועשרים, and the
  range), the phone read-out, price, counted nouns, and all five slang words. Best single piece
  of evidence: Soniox heard "מעשר עד שלוש" and normalized it to **"מ-10:00 עד 15:00"** — the
  production STT resolves the colloquial range exactly as intended, 12h→24h inference included.
  **Koren's ear check on `index-round5.html` still pending** (self-scoring page, r5-verdicts).
- **repeatedPhraseCount baseline** (30 usable reports of the 38 in the main worktree's
  `call-reports/`): median 0, **max 62** (reproduces §4's number exactly), 11 calls over the ≤2
  gate. Worst offenders: 2026-07-14T11-58 (62), 2026-07-14T11-05 (39). Full table via
  `node scripts/repeated-phrases-baseline.mjs "C:/AI Sales agent/call-reports"`.
- **Scenario suite, ledger ON (all 6 scenarios): repeatedPhraseCount = 1,0,0,1,0,0 — gate (≤2)
  passed.** Ledger note injected on 2 calls; on those calls prompt-cache hit stayed 93–94%
  (vs 92% ledger-off on the same scenario class) and **0** "preemptive generation … changed"
  warnings in either run — the turn-boundary injection point invalidates nothing and adds no
  latency (hot_lead deadAir med/p90: 775/1013 ms ledger-off → 724/821 ms ledger-on; same range,
  no regression). Normalizer interventions during the suite: 0 — the prompt-side change already
  makes the model write words, the code net had nothing to catch (its own proof is the unit
  tests + round 5).
- Full test suite green except the two pre-existing leads-module failures
  (`lead.service.test.ts` / `lead.routes.test.ts` — leads territory, untouched).
- `npm run build` clean.

## ⚠️ Incident: Koren's 20:34 test call hit my local dev worker

While I was starting `voice:dev` for the bench, a real **outbound call** (Koren's number,
20:34–20:37, room `call-out-ee6fa9df…`) was dispatched to my local worker instead of the cloud
agent — local workers and the cloud agent share one dispatch pool. Consequences: he heard the
local config (**DeepDub** TTS from this worktree's `.env`, not prod sonic-3.5), tools ran against
the **local** docker DB (capture_lead_info wrote there; **no call_learnings row / dashboard entry
exists for that call**), and teardown never completed (report JSON
`call-reports/2026-08-27T17-35-19-138Z.json` in this worktree is the only record). Silver
lining — the transcript shows the new features working on a real call: name question and all
discovery questions came out as fresh bank variants, `repeatedPhraseCount = 0`, cache 86%,
deadAir med 721 / p90 1041 ms. **Rule going forward: never leave `voice:dev` registered longer
than a test window, and say so before opening one.**

## ⚠️ NOT deployed — blocked on the main↔trunk voice divergence (architect decision)

The plan was to deploy after the bench. Two blockers found:

1. This branch's `scripts/deploy-agent.mjs` is the OLD one (always `--secrets-file`, which the
   2026-08-26 handoff bans). Main's version has the infra session's guards + deploy ledger.
2. Main's guards **deliberately refuse a crm-automation build**: `DEPLOY_MARKERS` require DID
   routing (`resolveCallIdentity` / `isDidRefusal`), which this branch lacks — the 2026-08-26
   mis-routed-lead incident is why. Meanwhile main lacks this branch's last 33 voice commits
   (instant-ack, rounds 3/4 pronunciation + emotion, today's three). The branches must be merged
   before ANY next deploy, in either direction.

I dry-ran the merge (scratch worktree): only 6 conflicted files, hunks are small (signature/import
unions), **but** the semantic composition is real work: main's `persona` parameterizes agent
identity/gender (`AgentPersona`, `renderIdentity`, `buildGreeting`), while my new discovery-bank
phrasings and register examples hardcode feminine-Keren forms ("עם מי אני מדברת", "מדברת קרן").
Under a male tenant persona those become wrong — the banks need the same gender-variant treatment
`agent_persona` gives everything else. That is the `agent_persona` cross-workstream contract, so I
did not improvise it.

### Questions for architect

- **Who merges main ↔ feature/crm-automation, and when?** Everything is deployable the moment the
  union exists. My side's composition requirement: the Step-2 banks + SPOKEN_REGISTER examples
  need persona-gender variants (or an explicit "Keren-only until parameterized" note).
- The suite ran with `tools_disabled reason=settings_read_failed` ×6 against local docker
  (run A had tools working; run B none). Environmental or the 2s settings timebox under
  back-to-back calls? Worth a look before the next local tool-flow test.

## Deliberately NOT covered (piece 1)

Non-round prices (1,250 ₪), agorot/decimals, millions, amounts > 10,000, currency-before-number
order, dates (skipped via month-name lookahead), numbers > 19 outside prices, "רבע ל"-style
countdowns beyond :45/:50/:55. Untouched beats wrong — extend `speech-numbers.he.ts` + its test
table + round-N verification when a real call surfaces a gap. Also deliberately NOT wrapped in
variation banks: the "respond exactly with" lines (hold, handoff, opt-out, unknown-question) and
the FAQ answers — exact by design.

## Still open (needs Koren)

1. **Ear check**: open `tests/hebrew-tts-niqqud-ab/index-round5.html`, listen, send the summary
   (the feminine hour forms are the ones to judge).
2. **Real-call gate**: one call listening for (a) an hour said in words, (b) no phrase repeated
   verbatim, (c) exactly one natural slang touch per reply at most. Latency will be reported as
   always — until the merge lands, this needs a coordinated window against a local worker, or
   the merged deploy.
3. Slang corpus check after a few real calls: does מעולה (already common in her speech) crowd out
   the rest of the bank? The ledger will show it.

---

# 2026-08-27 — VOICE (written by the integrations session)

⚠️ **Heads-up for the voice session: I edited `voice-livekit.service.ts`, which is your territory.**
Koren asked for it directly after a production incident. Collision check done first — details at
the bottom. One commit, `11ff507` on `feature/voice-dial-fail-loud`.

## The incident

**Outbound calling was dead in production, silently, and had been.** A test lead produced
`event="outbound_call_placed"` in the logs and no phone call.

Root cause: **`LIVEKIT_SIP_OUTBOUND_TRUNK_ID` was never set on the Railway service** — it had no
SIP variables at all. `initiateOutboundCall` read it, found nothing, and returned
`{ callId: 'skipped' }` *without throwing*:

```ts
const trunkId = this.env.LIVEKIT_SIP_OUTBOUND_TRUNK_ID;
if (!trunkId) return { callId: 'skipped' };   // ← returns
```

Nothing downstream inspected the result shape, so:

- `flow-executor.worker.ts:291` logged `outbound_call_placed` on every lead, and
- `POST /api/v1/calls/outbound` answered `{ ok: true, callId: 'skipped' }` to the dashboard.

Not Zadarma. The trunks were healthy the whole time: outbound `ST_8s6N3DqUVtWw`
(sip.zadarma.com, +972555070922), inbound `ST_V5CXE6L5539t`, dispatch rule `SDR_GHNgzGi4CB4n`,
agent `CA_azGQ9uaLxpot` deployed 2026-08-26.

**Inbound was never affected** — `LIVEKIT_SIP_INBOUND_TRUNK_ID` is read only by
`admin/sip-trunk.service.ts`; inbound routing happens on LiveKit's side via the dispatch rule.

### This makes the production call count unreliable

Local `.env` has the correct trunk id, so calls driven from a dev server always worked. Calls
driven from Railway never did. **Any "production call" on record was almost certainly initiated
from someone's machine, not from prod** — worth re-checking before
`phase-6-verification-checklist.md` Layer 6 (10 real calls) is judged.

## Fixed

**Config (already live).** Set on Railway service `AI-sales-agent`:
`LIVEKIT_SIP_OUTBOUND_TRUNK_ID=ST_8s6N3DqUVtWw` and `LIVEKIT_SIP_INBOUND_TRUNK_ID=ST_V5CXE6L5539t`.
Verified: the next test lead produced a genuinely connected call — room
`call-out-ee6fa9df…` with `agent-AJ_5PMBpvHx88eL` and `lead-+972509788845` both ACTIVE and
publishing. Note the failing attempt returned in milliseconds while the working one took ~10s,
because `waitUntilAnswered: true` actually waits for pickup — that timing gap is a usable signal.

**Code (`11ff507`, unmerged).**

1. `initiateOutboundCall` now **throws** `AppError(503, 'SIP_TRUNK_NOT_CONFIGURED')` instead of
   returning the skip marker, so a make_call step fails into the DLQ where someone finds it.
   Callers need no change — the retry/DLQ path already exists.
2. It also `console.error`s `event: 'dial_failed_no_trunk'` with tenantId + leadId — the throw is
   swallowed by BullMQ retry, so without the log the only trace is a job failure reason.
   Deliberately **not** logging `to`: that is a lead's phone number.
3. `server.ts` (additive) logs a loud `event: 'outbound_dialling_disabled'` **at boot** when the
   service constructs but no trunk id is set. The service is perfectly constructible without one —
   it is read only inside `initiateOutboundCall` — so a trunk-less instance looks healthy until a
   lead goes uncalled. This line is what would have caught the missing var on the deploy that
   dropped it.
4. New `voice-livekit.dial-config.test.ts` (5 cases), including one asserting the result can never
   again be the success-shaped `{ callId: 'skipped' }`, and one treating an empty-string trunk id
   as unset (Railway returns `''` for a cleared variable).

Full suite green: 95 files / 970 tests. `npm run build` clean.

## Collision check before touching your file

- Your working tree (`C:/keren-voice`, `feature/crm-automation`) had 13 uncommitted files:
  migrations + ElevenLabs audio samples. Nothing near the dialer or flow executor. **Untouched.**
- Branches with unmerged commits to `voice-livekit.service.ts`: `feature/voice-rag-r1` (+3) and
  `feature/voice-cartesia-tenant-tts` (+5). Both only append entries to `AGENT_SETTINGS_KEYS`
  around L174–194 (`knowledge_base`, `agent_persona`). My change is at L68 and in the doc comment
  above it. **No overlap — your rebases should be clean.**
- The LiveKit agent was **not** redeployed. `scripts/deploy-agent.mjs` was never run, so the
  ledger guard from `f20a1a4` is untouched and no unmerged agent build was replaced.

## Worth deciding (not mine to call)

- **Same pattern elsewhere?** `initiateOutboundCall` was not uniquely at fault — the shape is
  "missing config → return a success-ish value → caller logs success". Worth a sweep for other
  early returns on unset env in the voice path.
- **`POST /calls/outbound` now surfaces a 503** where it used to return `ok: true`. That is the
  point, but check the dashboard renders it as an error rather than a silent no-op.
- **A prod smoke test for dialling** would have caught this independently of anyone waiting for a
  phone to ring — e.g. the boot check above escalated to a health-check field.

> *(Merge note 2026-08-28: two sessions wrote a 2026-08-27 voice handoff — the voice session's own (top) and the integrations session's dial-fail-loud incident report (bottom). Both kept in full.)*
