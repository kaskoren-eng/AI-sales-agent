# Voice — 2026-08-23: first call on the four fixes

Call `2026-08-23T08-47-12-491Z.json`, 210s, 14 turns, tenant `6b05cd71`. First real exercise of
`b82c2e0`, `61c4d2a`, `64f5284`, `2ac656a`.

## Scoreboard

| fix | verdict |
|---|---|
| #1 prefetch reuse | **works.** 17 of 23 slots reused; median wait **239ms → 2ms** |
| #2 short questions | **not exercised.** No `answering_agent` skip occurred; still unverified |
| #3 answer brevity | **reverted, and the goal withdrawn** — see Open #2. Cost 216 tokens/inference for no measured change |
| #4 duplicate detector | **nothing to detect.** No repeat this call |

The pricing question — the one she failed last time — was answered correctly at 23s with the real
figures. That specific regression is closed.

## The new problem: the rolling slot loses a fragmented question

She was asked whether the agent works on Shabbat and evenings and answered *"אין לי כרגע את המידע
הזה"*. **The KB has that answer** — a direct probe scores the availability chunk **0.576** on her own
garbled transcript and **0.688** on the clean phrasing. Retrieval was never the problem.

What happened is that STT split the question across four turns:

```
138s  "מה? איך."                                    -> too_short, then a slot that EXPIRED empty
145s  "זה עובד, הסוכן? ... הוא עובד גם בשבת, ב—"    -> slot, 3 chunks
146s  "בשעות לילה, בערב. איך זה עובד?"              -> slot, 3 chunks
148s  "בהקשר הזה."                                  -> slot, 3 chunks (for a meaningless tail)
163s  she answers, un-grounded
```

The slot is rebuilt from the **last committed user turn** and lives for exactly one inference. When
the question arrives in pieces, the fragment that reaches the final inference is a tail like
"בהקשר הזה" — and the slot that actually held the answer was discarded two turns earlier.

**This is a cost of R2.1 specifically.** Under R2's cumulative injection those chunks were still in
context when she finally spoke. The context diet traded that away. Same mechanism at 65s: fragments
at 63/64/64, then an expired empty slot at 65s, then a 61-word un-grounded answer.

This call had **6 fragmented turns of 14**. It is now the top RAG issue, ahead of threshold tuning.

Candidate directions, none built: build the slot from the last N user turns rather than the last one;
or carry the previous slot forward when the current turn is a fragment; or merge fragments before
they reach `llmNode`.

## Latency

| | 2026-08-22 (8 min) | 2026-08-23 (3.5 min) |
|---|---|---|
| EOU | 595ms | **230ms** |
| LLM TTFT | 922ms | **1175ms** |
| TTS TTFB | 508ms | 533ms |
| response ≈ | 2025ms | 1938ms |
| prompt cache | 87% | 83% |

**TTFT went UP by ~250ms, and that needs explaining rather than excusing.** The 239ms slot wait was
*inside* the TTFT measurement (`resolve` runs in `llmNode` before `super.llmNode`), so removing it
should have cut TTFT by roughly that much. It rose instead — a ~490ms swing unaccounted for.
Candidates: the +185 words fix #3 added to the resident prompt, the lower cache hit rate, or ordinary
variance between two different conversations. **One call each side is not a controlled comparison**
and this should not be reported as a regression until it is one.

## Pick-up delay

Koren reports she takes too long to answer, twice now. The log shows **~11.5s between the
participant's tracks appearing (11:47:12.704) and the first speech handle (11:47:24.231)**. How much
of that is SIP negotiation versus our own startup is not established. Needs its own measurement —
do not guess at it.

## Still open

1. **Fragmented questions defeat the rolling slot** (above). Top priority.
2. ~~**Answer length.**~~ **CLOSED 2026-08-23, not a defect.** Koren, who judges these calls by ear:
   *"the long answers are not necessarily wrong or bad — in some cases the reply of the agent must be
   longer than usual. until now I didn't feel like it's too long."* Counting words in a transcript was
   a metric being mistaken for a judgement. Do not reopen unless he asks, about specific answers he
   heard.
3. **Pick-up delay** — unmeasured.
4. **2 of 23 slots still expired (9%)**, both on fragment tails — likely the same root cause as #1.
5. **2 preemptive drafts discarded** by context change, of 14. Both traced: one follows an
   interrupted playout, one a tool call. Neither is the injection. The verifier's barge-in
   attribution only recognises `speech interrupted, new user turn detected` and should be widened —
   carefully, since widening a filter until it passes is how a check stops meaning anything.
6. **Environment:** `docker-compose.yml` describes port 5432, `.env` uses 55432, and the real
   container `keren-rag-pg` is hand-created outside compose on an **anonymous volume**. A stray
   `docker rm` would orphan the knowledge base with no named volume to recover from.
