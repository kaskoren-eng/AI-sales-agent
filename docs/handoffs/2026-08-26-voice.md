# 2026-08-26 — voice: Hebrew pronunciation polish (round 3)

## What shipped (feature/crm-automation)

- **`speech-guard.ts` — the round-3 pronunciation winners.** Masculine suffix table moved from the
  שלכה respelling to minimal niqqud (שלך → שלךָ, one kamatz — beat the respelling AND plain in the
  sonic-3.5 listening A/B). New feminine table (per-word winners: לָךְ, שלאך, אלייך, …). New
  gender-neutral pronunciation dictionary, first entry לוודא → לוודֵא (the swallowed final vowel).
  `stripNiqqud` reordered to run BEFORE the tables — it now removes only model-emitted pointing,
  never our verified marks; the order is pinned by a test.
- **`AddressGenderTracker`** (+ wiring in `agent.ts::ttsNode`): masculine by default, flips to
  feminine only on the LLM's own unambiguous feminine conjugation (curated תרצי/תוכלי list, or
  את + present verb), one-way by design (masc 2sg future ≡ 3sg fem — "היא תוכל" must not flip it
  back). Per-call state, zero prompt changes, zero latency cost (regex on text already in flight).
- **Evidence committed** in `tests/hebrew-tts-niqqud-ab/`: round-3/3b scripts + WAVs (sonic-3.5),
  `roundtrip.ts` (44.1k → 8kHz phone → Soniox: 27 clips, every candidate heard back as the intended
  word), and the previously-untracked July round-2 files (D/E/F minimal-niqqud round).
- **`docs/phase-4-known-issues.md` §13 amended**: "niqqud is dead" now correctly says *blanket*
  niqqud is dead; *single-mark* niqqud is the shipped fix. Both experiments cited.

## Verdicts this was built on (Koren, 2026-08-26, sonic-3.5)

round 3: vd1 C · vd2 C · m1 C · m2 C · f1 C · f2 B · ps1-3,5 ok · ps4 flagged (see below).

## Open / pending

-2. **Round 4b — the wider palette + THE LAUGHTER FINDING.** Written laughter is DEAD on Hebrew
   sonic-3.5: "חח" read as the letter khet, "חהחה" spelled out, "אוו" swallowed (w1/w2/w4 bad).
   Passed: אוף... (sigh), איזה כיף (joy), וואלה? (surprise). `EMOTIONAL_COLOR` now carries the
   full 7-device palette and explicitly BANS written laughter (LLMs love typing חחח); amusement
   is said in words. Deployed as Tri7uyV2mxoG.
-1. **Round 4 — emotion (evening).** Cartesia inline tags do NOTHING on Hebrew sonic-3.5
   ([laughter]/[sigh] silently ignored — Koren: "the tags are doing nothing", matching Cartesia
   support). Emotion travels in the TEXT: verdicts p1=C (interjection+!), p2=B (ellipsis),
   p3=C (either/or question melody — answers the ps4 flat-question complaint), e1/e2=B (colored
   phrasing). New `EMOTIONAL_COLOR` system-prompt section teaches exactly those three devices —
   one touch per reply max, inside the reply (instant-ack double-receipt protected), tags banned.
   Pinned by 4 new prompt tests. Evidence: `index-round4.html`, 16 clips.
0. **Evening addition — רוצה** (Koren's request after live testing): subject-aware entry — אתה
   רוצֶה / את רוצָה by addressee, אני/היא רוצָה + הוא רוצֶה subject-side ("אני רוצה" = the agent,
   feminine until agent_persona parametrizes it), מרוצה protected. Round-3d verdicts: rm/rf/rs
   all C — the marks confirmed by ear + Soniox round-trip. Also that evening: the gender tracker
   was rebuilt bidirectional (latest-signal-wins + caller self-identification hook) after the
   first live call exposed the one-way design — see the commit for the transcript-derived tests.
1. **All verdicts in — the tables are FINAL.** 3b: bm1=C bm2=C bm3/4/5=none, bf1=B bf2=C bf3=B
   bf4=C bf5=C (feminine corrected to אותאך, אלַיִךְ, איתאך). 3c: c1/c2/c3 = B — the כה respelling
   beat the patach for masculine איתך/בשבילך/עבורך, confirming the revert already in the table.
   Net: masculine = kamatz on לך/שלך/אותך/אליך + כה respelling on the other three; feminine =
   per-word mix. Every entry ear-picked AND Soniox round-tripped. Nothing pending on the tables.
2. **Real-call check.** Next production/test call: listen for לוודא, לך/שלך both genders; report
   latency numbers as always. The change is text-level only — no latency impact expected.
3. **ps4 observation (out of scope here):** robotic question intonation — flat pitch at the end of
   "באיזו שעה נוח, ויש שאלות שכדאי להכין?" Words pronounced correctly; it's prosody, not
   pronunciation. Parked — candidate for a punctuation/phrasing experiment later.
4. **Pre-existing test failures (not mine, not touched):** `lead.service.test.ts` /
   `lead.routes.test.ts` — "(intermediate value) is not iterable" at `lead.service.ts:39` (mock
   shape vs `select({ value: count() })`). Present before this session's changes; leads-module
   territory.
5. **Untouched leftovers in this worktree** (someone's ElevenLabs probe): modified `livekit.toml`,
   `src/db/migrations/meta/_journal.json`, untracked `el-sample-*.mp3`, `.agent-secrets.env.bak-preEL`,
   `0006_snapshot.json`. Left as-is per territory rules.

## Questions for architect

- None blocking. FYI: the פ/ש screening (5 sentences of frequent sales words) found **zero**
  consonant misreadings on sonic-3.5 — the פ/ש issue may already be solved by the 3.5 upgrade;
  waiting on Koren's real-call word list before adding anything.
