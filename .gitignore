node_modules/
dist/
.env
*.log
.DS_Store
.vscode/
*.tsbuildinfo
.claude/
screenshots/

# Voice A/B samples — regenerate with `npm run voice:ab`
voice-samples/

# Call reports — contain caller phone numbers and transcripts (PII). Never commit.
call-reports/

# Agent deploy secrets — never commit
.env.agent
.agent-build/
.agent-secrets.env
